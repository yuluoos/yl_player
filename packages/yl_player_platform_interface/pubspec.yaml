name: yl_player_platform_interface
description: Shared platform contract and immutable value types for the federated yl_player Flutter plugin.
version: 0.1.0-dev.1
resolution: workspace

environment:
  sdk: ^3.12.0
  flutter: '>=3.44.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.8

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

